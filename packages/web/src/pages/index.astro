---
import Layout from "../layouts/Layout.astro";

import { listTopFive } from "../../../core/src/top" 

const [topFive] = await Promise.all([listTopFive()])
console.log('topFive: ', topFive.map(i => i));
---

<Layout title="Abandoned Products Hunt">
  <main>
    <h1>Welcome to <span class="text-gradient">Astro</span></h1>
    <p class="instructions">
      {topFive.map(item => item.topfive)}
    </p>
  </main>
</Layout>
